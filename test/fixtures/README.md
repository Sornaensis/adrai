# Test fixtures

This directory will own fixtures used by Haskell tests, including parity inputs whose provenance is documented without committing generated or prototype-owned artifacts. Fixtures are future work.
