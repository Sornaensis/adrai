# Search

The scaffold checks only that the linked SQLite library accepts an FTS5 virtual table. It does not implement search. Query semantics, ranking, indexing, and operational limits will be documented with the Haskell search implementation.
